import { IsBrowserRenderDirective } from './is-browser-render.directive';

describe('IsBrowserRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new IsBrowserRenderDirective();
    expect(directive).toBeTruthy();
  });
});
